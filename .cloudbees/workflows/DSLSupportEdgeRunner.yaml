apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
   workflow_dispatch :

jobs:
  build:
    runs-on: LinuxEc2
    steps:
      - name: Say hello
        shell: sh
        run: |
          sleep 9000
          echo "hello world HARI"