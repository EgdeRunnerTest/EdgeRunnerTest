apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  build:
    runs-on: [os:darwin]
    steps:
      - name: Say hello
        shell: sh
        run: |
          echo "hello world HARI"