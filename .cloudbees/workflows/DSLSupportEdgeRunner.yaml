apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
   push

jobs:
  build:
    runs-on: LinuxEc2
    steps:
      - name: Say hello
        shell: sh
        run: |
          echo "hello world HARI"