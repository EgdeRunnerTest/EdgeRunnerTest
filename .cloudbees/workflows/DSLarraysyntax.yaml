apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: [hari]
    steps:
      - name: Say hello
        shell: sh
        run: |
          echo "hello world HARI"