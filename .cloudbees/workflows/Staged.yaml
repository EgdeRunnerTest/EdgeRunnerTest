apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
metadata:
  stages/v1alpha1:
    - name: Stage 1
      jobs:
        - job2
jobs:
  job2:
    runs-on: LinuxEc2
    steps:
      - name: Hello world
        uses: docker://golang:1.20.3-alpine3.17
        run: echo "job1"
        kind: build
        shell: sh
