apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: neg-undefined-expression
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ secrets.NONEXISTENT_VAR }}

    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
